# backend

Carpeta destinada al código server-side, transformaciones y procesamiento de datos.

Contenido recomendado:

- Scripts y servicios (por ejemplo: Python scripts, APIs en Node/Flask/FastAPI)
- Instrucciones de ejecución (README con dependencias, comandos y entorno virtual)

Ejemplos de archivos que deberían residir aquí:

- apply_module02_overlay.py
- cualquier script que corra en el servidor o procese datos en lote
